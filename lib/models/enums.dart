/// Reading page tint applied over rendered PDF pages.
enum PageTint { paper, sepia, night }

/// Library layout.
enum LibraryView { grid, list }

/// Library sort order.
enum SortMode { recent, name, dateAdded }

/// App theme mode (maps to Flutter's [ThemeMode]).
enum AppThemeMode { system, day, night }
